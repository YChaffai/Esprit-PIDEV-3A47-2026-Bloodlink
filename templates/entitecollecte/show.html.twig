{% extends 'base.html.twig' %}

{% block title %}Entitecollecte{% endblock %}

{% block body %}
    <h1>Entitecollecte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entitecollecte.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entitecollecte.nom }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ entitecollecte.localisation }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ entitecollecte.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entitecollecte_index') }}">back to list</a>

    <a href="{{ path('app_entitecollecte_edit', {'id': entitecollecte.id}) }}">edit</a>

    {{ include('entitecollecte/_delete_form.html.twig') }}
{% endblock %}
