{% extends 'base_front.html.twig' %}

{% block title %}Mes Dons | BloodLink{% endblock %}

{% block body %}
  <div class="py-4 py-md-5">

    <div class="d-flex justify-content-between align-items-end flex-wrap gap-3 mb-4">
      <div>
        <div class="small mb-1" style="color:var(--muted); letter-spacing:.18em;">BLOODLINK</div>
        <h1 class="fw-black mb-1" style="letter-spacing:-.6px;">
          <i class="fa-solid fa-hand-holding-droplet me-2" style="color:var(--primary);"></i>
          Mes Dons
        </h1>
        <div style="color:var(--muted);">Tout votre historique de dons au même endroit.</div>
      </div>
    </div>

    {% if dons|length > 0 %}
      <div class="glass-card overflow-hidden">
        <div class="p-4 d-flex justify-content-between align-items-center"
             style="background:rgba(0,0,0,.35); border-bottom:1px solid var(--border);">
          <div class="fw-black">Historique</div>
          <div class="small" style="color:var(--muted);">{{ dons|length }} enregistrements</div>
        </div>

        <div class="table-responsive">
          <table class="table table-dark mb-0 align-middle"
                 style="--bs-table-bg: transparent; --bs-table-border-color: var(--border);">
            <thead>
              <tr style="color:var(--muted);">
                <th class="py-3">Date</th>
                <th class="py-3">Type</th>
                <th class="py-3">Quantité</th>
                {# ✅ Action Column Added #}
                <th class="py-3 text-end">Actions</th>
              </tr>
            </thead>
            <tbody>
              {% for don in dons %}
                <tr style="border-top:1px solid var(--border);">
                  <td class="py-3">
                    {% if don.date %}
                      <span class="badge rounded-pill px-3 py-2"
                            style="background:rgba(255,255,255,.06); border:1px solid var(--border); color:white;">
                        {{ don.date|date('d/m/Y') }}
                      </span>
                    {% else %}
                      —
                    {% endif %}
                  </td>

                  <td class="py-3">
                    <span class="badge rounded-pill px-3 py-2"
                          style="background:rgba(230,57,57,.14); border:1px solid rgba(230,57,57,.25); color:white;">
                      {{ don.typeDon ?? '—' }}
                    </span>
                  </td>

                  <td class="py-3">
                    {% if don.quantite %}
                      <strong>{{ don.quantite }}</strong> <span class="small" style="color:var(--muted);">ml</span>
                    {% else %}
                      —
                    {% endif %}
                  </td>

                  {# ✅ Delete Button Added #}
                  <td class="py-3 text-end">
                    <form method="post" action="{{ path('front_don_delete', {'id': don.id}) }}"
                          class="d-inline" onsubmit="return confirm('Supprimer ce don ?');">
                      <input type="hidden" name="_token" value="{{ csrf_token('delete_don_' ~ don.id) }}">
                      <button class="btn btn-sm rounded-pill fw-bold"
                              style="background:rgba(230,57,57,.12); color:white; border:1px solid rgba(230,57,57,.25);">
                        <i class="fa-solid fa-trash me-1"></i> Supprimer
                      </button>
                    </form>
                  </td>
                </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    {% else %}
      <div class="glass-card p-5 text-center">
        <div class="mx-auto mb-3 rounded-circle d-flex align-items-center justify-content-center"
             style="width:76px;height:76px;background: rgba(230,57,57,.12); border: 1px solid rgba(230,57,57,.25);">
          <i class="fa-solid fa-hand-holding-droplet fs-2" style="color:var(--primary);"></i>
        </div>

        <h4 class="fw-black mb-2">Aucun don enregistré</h4>
        <p class="mb-4" style="color:var(--muted);">Votre historique apparaîtra ici une fois que vous aurez effectué un don en centre.</p>
      </div>
    {% endif %}
  </div>
{% endblock %}