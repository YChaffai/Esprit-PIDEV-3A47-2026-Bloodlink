{% extends 'back/baseBack.html.twig' %}
{% block title %}Don #{{ don.id }} | BloodLink Admin{% endblock %}

{% block body %}
<div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-4">
  <div>
    <h1 class="fw-black text-uppercase mb-1" style="letter-spacing:-1px;">Don #{{ don.id }}</h1>
    <p class="text-white-50 mb-0">Détails du don.</p>
  </div>

  <div class="d-flex gap-2">
    <a href="{{ path('back_don_edit', {id: don.id}) }}" class="btn btn-outline-light rounded-pill px-4 fw-bold">
      <i class="fa-solid fa-pen me-2"></i> MODIFIER
    </a>
    <a href="{{ path('back_don_index') }}" class="btn btn-outline-light rounded-pill px-4 fw-bold border-secondary border-opacity-25" style="background: rgba(255,255,255,0.03);">
      <i class="fa-solid fa-arrow-left me-2"></i> RETOUR
    </a>
  </div>
</div>

<div class="glass-card p-4 p-md-5">
  <div class="row g-4">
    <div class="col-md-6">
      <div class="text-white-50 small">Donneur</div>
      <div class="fw-bold">
        {% if don.client and don.client.user %}
          {{ don.client.user.nomComplet }}
        {% else %}
          Client #{{ don.client ? don.client.id : '—' }}
        {% endif %}
      </div>
    </div>

    <div class="col-md-6">
      <div class="text-white-50 small">Date</div>
      <div class="fw-bold">{{ don.date ? don.date|date('Y-m-d H:i') : '—' }}</div>
    </div>

    <div class="col-md-6">
      <div class="text-white-50 small">Type</div>
      <div><span class="badge badge-premium">{{ don.typeDon }}</span></div>
    </div>

    <div class="col-md-6">
      <div class="text-white-50 small">Quantité (ml)</div>
      <div class="fw-bold">{{ don.quantite }}</div>
    </div>
  </div>
</div>
{% endblock %}
