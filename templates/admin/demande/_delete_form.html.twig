<form method="post" action="{{ path('back_demande_deletee', {'id': demande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande.id) }}">
    <button class="btn">Delete</button>
</form>
