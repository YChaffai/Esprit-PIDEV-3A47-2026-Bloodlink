{% extends 'back/baseBack.html.twig' %}

{% block title %}Compléter profil Donneur | BloodLink Admin{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-lg-9 col-xl-8">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-5">
            <div>
                <h1 class="fw-black text-uppercase mb-1" style="letter-spacing:-1px;">Compléter Profil</h1>
                <p class="text-white-50 mb-0">Informations de santé pour le compte #{{ user.id }}.</p>
            </div>
        </div>

        <div class="glass-card p-4 p-md-5" style="background: rgba(0, 0, 0, 0.4);">
            <div class="mb-5">
                <h2 class="fw-black text-uppercase mb-1" style="letter-spacing:-0.5px;">
                    <i class="fa-solid fa-heart-pulse me-2" style="color:var(--primary);"></i>Donneur
                </h2>
                <p class="text-white-50">Saisie des informations de santé essentielles.</p>
            </div>

            {{ form_start(form, { attr: { novalidate: 'novalidate' } }) }}
                <div class="row g-4">
                    <div class="col-md-6">
                        <label class="form-label fw-bold small text-uppercase" style="color:var(--muted); letter-spacing:1px;">Groupe Sanguin</label>
                        {{ form_widget(form.typeSang, { attr: { class: 'form-select form-control-premium border-0' ~ (form.typeSang.vars.errors|length ? ' is-invalid' : '') } }) }}
                        {% for error in form.typeSang.vars.errors %}
                            <div class="mt-2 d-flex align-items-center bg-danger bg-opacity-10 text-danger p-2 rounded-3 small border border-danger border-opacity-25">
                                <i class="fa-solid fa-circle-exclamation me-2"></i>{{ error.message }}
                            </div>
                        {% endfor %}
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-bold small text-uppercase" style="color:var(--muted); letter-spacing:1px;">Date du dernier don</label>
                        {{ form_widget(form.dernierDon, { attr: { class: 'form-control form-control-premium border-0' ~ (form.dernierDon.vars.errors|length ? ' is-invalid' : '') } }) }}
                        {% for error in form.dernierDon.vars.errors %}
                            <div class="mt-2 d-flex align-items-center bg-danger bg-opacity-10 text-danger p-2 rounded-3 small border border-danger border-opacity-25">
                                <i class="fa-solid fa-circle-exclamation me-2"></i>{{ error.message }}
                            </div>
                        {% endfor %}
                    </div>
                </div>

                <div class="d-flex justify-content-end mt-5">
                    <button type="submit" class="btn btn-premium px-5">
                        CONFIRMER <i class="fa-solid fa-arrow-right ms-2"></i>
                    </button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
