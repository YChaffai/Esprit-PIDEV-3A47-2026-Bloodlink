{% extends 'base_front.html.twig' %}

{% block title %}Créer un utilisateur{% endblock %}

{% block body %}
  <div class="row justify-content-center mt-4">
    <div class="col-lg-9 col-xl-8">

      <div class="glass-card p-4 p-md-5">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
          <h2 class="m-0" style="font-weight:900;">
            <i class="fa-solid fa-user-plus me-2" style="color:var(--primary);"></i>
            Ajouter un utilisateur
          </h2>

          <a href="{{ path('app_user_index') }}" class="btn btn-outline-light rounded-pill">
            <i class="fa-solid fa-arrow-left me-2"></i>Retour à la liste
          </a>
        </div>

        {# Inclusion du formulaire avec les labels en français #}
        {{ include('user/_form.html.twig', {
          button_label: 'Créer le compte',
          title: 'Informations du nouvel utilisateur'
        }) }}
      </div>

    </div>
  </div>
{% endblock %}
