{# <!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
            <script>
                document.addEventListener("DOMContentLoaded", function () {
                    flatpickr(".datepicker", {
                        dateFormat: "Y-m-d H:i",  // Format de la date et de l'heure
                        minDate: "today",         // Empêcher la sélection des dates passées
                        enableTime: true,
                        time_24hr: true,
                        allowInput: false,
                        onOpen: function(selectedDates, dateStr, instance) {
                            // Désactive la saisie clavier dans les champs heure et minute du popup
                            instance.hourElement.setAttribute("readonly", "readonly");
                            instance.minuteElement.setAttribute("readonly", "readonly");
                            
                        },
                        //minTime: "08:00",
                        //maxTime: "17:00",
                        disable: [
                            function (date) {
                                return (date.getDay() === 0 || date.getDay() === 6);  // Samedi et Dimanche
                            },
                            "2026-01-01",  // Jour de l'an
                            "2026-12-25"   // Noël
                        ],
                        
                    
                       
                    });
                });
            </script>
        {% endblock %}

    </head>
    <body>
    <div class="container mt-3">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert" style="padding: 15px; margin-bottom: 20px; border: 1px solid transparent; border-radius: 4px; {{ label == 'danger' ? 'color: #721c24; background-color: #f8d7da; border-color: #f5c6cb;' : 'color: #155724; background-color: #d4edda; border-color: #c3e6cb;' }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
        {% block body %}
        

    
        
        {% endblock %}
    </body>
</html> #}


{# templates/base.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}BloodLink{% endblock %}</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" rel="stylesheet">
    
    {# Ajout de Flatpickr pour vos formulaires de rendez-vous #}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/themes/dark.css">

    <style>
        :root{
            --primary:#e63939;
            --bg:#0a0a0a;
            --bg:#0a0a0a;
            --surface:#111;
            --text:#f8f8f8;
            --muted:rgba(255,255,255,.65);
            --border:rgba(255,255,255,.08);
            --overlay-color: rgba(10,10,10,.86);
        }
        body{ background:var(--bg); color:var(--text); font-family:'Montserrat',sans-serif; margin:0; overflow-x:hidden; }

        .header{
            position:fixed; top:0; left:0; right:0; z-index:1000;
            padding:18px 5%; background:rgba(10,10,10,.75); backdrop-filter:blur(15px);
            border-bottom:1px solid var(--border);
        }
        .nav-logo{
            font-size:1.65rem; font-weight:900; letter-spacing:-.5px;
            color:white; text-decoration:none; display:flex; align-items:center; gap:12px;
        }
        .nav-logo i{ color:var(--primary); }

        .menu-toggle-btn{
            background:none; border:none; color:white; font-weight:800; text-transform:uppercase;
            letter-spacing:1px; display:flex; align-items:center; gap:10px;
            cursor:pointer; font-size:.9rem;
        }
        
        .main-container{ margin-top:120px; min-height:80vh; padding:0 5% 50px 5%; }

        /* Style pour vos formulaires et listes existants */
        .glass-card{
            background:rgba(255,255,255,.03);
            border:1px solid var(--border);
            border-radius:18px;
            padding: 30px;
            box-shadow:0 20px 60px rgba(0,0,0,.35);
            margin-bottom: 25px;
        }
        
        h1 { font-weight: 800; text-transform: uppercase; letter-spacing: -1px; margin-bottom: 30px; color: var(--primary); }
        hr { border-color: var(--border); opacity: 1; }
        
        /* Adaptation des tableaux et inputs */
        input, select, textarea { 
            background: var(--surface) !important; 
            border: 1px solid var(--border) !important; 
            color: white !important;
            border-radius: 8px !important;
        }
        .btn-primary { background: var(--primary); border: none; font-weight: 700; padding: 12px 25px; border-radius: 50px; }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body>

<header class="header d-flex justify-content-between align-items-center">
    <a href="#" class="nav-logo">
        <i class="fas fa-droplet fa-lg"></i> BLOODLINK
    </a>
    <button id="menu-toggle" class="menu-toggle-btn">
        MENU <span class="ms-1" style="font-size:1.2rem;">☰</span>
    </button>
</header>

{# Le menu overlay reste identique au design BloodLink #}

<main class="main-container">
    {# Gestion des messages Flash unifiée #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label == 'error' ? 'danger' : label }} border-0 bg-{{ label == 'error' ? 'danger' : label }} bg-opacity-25 text-white mb-4 glass-card">
                <i class="fas {{ label == 'success' ? 'fa-check-circle' : 'fa-triangle-exclamation' }} me-2"></i> {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    {% block body %}{% endblock %}
</main>

<script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>

<script>
    // Script du Menu (GSAP simple)
    // ... (Gardez votre script GSAP ici) ...

    // Initialisation Flatpickr pour vos formulaires de RDV
    document.addEventListener("DOMContentLoaded", function () {
        flatpickr(".datepicker", {
            dateFormat: "Y-m-d H:i",
            enableTime: true,
            time_24hr: true,
            minDate: "today",
            theme: "dark"
        });
    });
</script>

{% block javascripts %}{% endblock %}
</body>
</html>